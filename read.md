for fiverr_keeper_sb

install chrome first
```aiignore
sudo apt update
sudo apt install wget -y
wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
sudo apt install ./google-chrome-stable_current_amd64.deb -y

sudo apt install -y xvfb

for ubuntu 24.04

sudo apt install -y \
  wget unzip xvfb libxi6 libnss3 libxss1 libappindicator3-1 \
  libasound2t64 fonts-liberation libatk-bridge2.0-0t64 libgtk-3-0t64 \
  libx11-xcb1 libdrm2 libgbm1 libxdamage1 libxcomposite1 \
  libxrandr2 libxkbcommon0 libu2f-udev libvulkan1 xdg-utils
```